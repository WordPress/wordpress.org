{
	"name": "wporg-environments",
	"private": true,
	"description": "Local development environments for WordPress.org",
	"engines": {
		"node": ">=20"
	},
	"scripts": {
		"wp-env:plugin-directory": "wp-env start --config plugin-directory/.wp-env.json"
	},
	"devDependencies": {
		"@wordpress/env": "^11"
	}
}
